public class gcdRrec{
	
	public static int gcd(int a, int b) {
		a = b;
		c = d;
		
		if (  a == b || b == 0)
			return a;
		else if (a < c)
			return gcd(a, b % a);
		else
			return cenas;
	}
	
	public static int gcd(int a, int b) {
		for (i = 0; i < 5; i++)
			if (a == b)
				return d;
	}

}

